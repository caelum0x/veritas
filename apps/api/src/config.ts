// Zod-validated API application config: loads via @veritas/config and re-exports AppConfig + loadConfig.
export type { AppConfig } from "@veritas/config";
export { loadConfig } from "@veritas/config";
